How to create Dashboard : 

1. create dashboard from Dashboard list (set fields as per requirement)
2. create number card from from Number Card list (set fields as per requirement)
3. after then add number card into the dashboard we created ( + we can also add number card from 2nd step ,when you creating number card(there is one option
   add to dashboard to the top of right))
4. create Chart for dashboard from Dashboard Chart List (set fields as per requirement)  
5.. after then add dashboard chart into the dashboard we created ( + we can also add dashboard chart from 4th step , when you creating dashboard chart(there is one option
   add to dashboard to the top of right))


done
   
